<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/CHANGELOG.md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/CHANGELOG.md" />
              <option name="originalContent" value="# Changelog&#10;&#10;## CD.04.06&#10;- Fixed small things that were pointed out by GCC 2.8.x&#10;- Fixed one more memory leak, pointed out by Cygnus&#10;- Changed how sockets are made non-blocking to not repeat common code many times.&#10;- Make `array - ({ 0 })` and `-= ({ 0 })` expressions actually remove all destructed objects in arrays too; from Cygnus&#10;- Added statistics for cleanup performance; from Cygnus&#10;- Made `?:` expressions with an empty second part legal, if empty it will take on the value from the first part; from Azid@Kingdoms.&#10;&#10;## CD.04.05&#10;- Made `functionp()` return 0 when passed a pointer to a function in a destructed object.&#10;- Added `function_object()` to extract the object from a function pointer.&#10;- After a catch of evaluation cost overflow the evaluation cost is reset to slightly below maximum if this was in the outermost `catch()`. The reserve is defined by `EXTRA_COST` in config.h.&#10;- Allow using parenthesized constant strings in inherit; from Plarry@Muddy_Waters.&#10;- Put back code to call `clean_up()` in objects before swapping them out, to give them a chance to remove themselves.&#10;- Added an auto-inherited object `/secure/auto`. This object is automatically inherited into _all_ objects, including `/secure/master`. Most of the code by Azid@Kingdoms&#10;- Added `efun::min()` and `efun::max()`; from Azid&#10;- Added `efun::function_name()` to get a string representation of a function pointer.&#10;- Made the not-operator `!` understand about destructed objects and function pointers pointing at functions in destructed objects.&#10;- Removed `efun::cindent()` and `util/indent`; it was both sorely out of date and did not make much sense to keep.&#10;- Removed `efun::query_inherit_list()`; it is available as `efun::debug(&quot;inherit_list&quot;)`&#10;- Removed the reserved word and attribute `protected`; it was not used.&#10;- Told the indenter in ed about some preprocessor directives; from Skywise&#10;- Moved function composition into the driver; from Cygnus@Synergy.&#10;- `operator(!)` and `operator(~)` now works; from Skywise&#10;- Get better textual function pointer representations; from Skywise&#10;- Allow master to insert lines at the start of new files; from Skywise&#10;- Optimized jumps in loops and switch statements; from Skywise&#10;- Optimized jumps to jumps; from Cygnus&#10;- Use `snprintf()` many places to prevent overflowing buffers; add a small version of `snprintf()` for systems that might not have it.  Add regression test for the problem this fixes.&#10;- Only enable `efun::break_point()` when the driver is compiled with DEBUG&#10;- Added floating point formats to `efun::speintf()`; from Pippin@Synergy&#10;- Corrected a seldom-occuring condition where we would loop forever.&#10;- Don't print last line twice in &quot;ed&quot; when doing substitution if both the general print flag is set and the &quot;p&quot; flag is given on the substitute command.&#10;- Define DEBUG_DRIVER preprocessor symbol if driver has been compiled with DEBUG enabled.&#10;- Make `efun::min()` and `efun::max()` take a variable number of arguments, at least two, and return the minimum/maximum of all values passed.  From Cygnus@Synergy.&#10;- Make the | operator do array unions; from Cygnus@Synergy.&#10;- Check for stack overflow in macro version of `push_svalue()`.&#10;- Don't die if a function is redeclared with different number of arguments when running in DEBUG mode.&#10;- Don't special-case shadowing of `catch_tell()` in the driver; that should be done in the mudlib if at all.&#10;- `efun::call_self` and `efun::call_selfv` no longer cause the stack to be mangled, restore them and add a regression test to verify that they work properly&#10;- Add implementation of `strerror()` for systems that do not provide it&#10;&#10;## CD.04.04&#10;- Fixed a problem where the driver would crash when getting the command list for a living object if any of the verbs had an efun connected to them (and thus no object defined)&#10;- Optimized SUPER_SNOOP code; from Azid.&#10;- Allow &quot;void&quot; as an argument, indicating no arguments are allowed like ANSI. From Azid.&#10;- Made `sprintf()` generate runtime errors when buffer overflows.&#10;- Add limits to number of concurrent alarms per object; default to 128.&#10;- Generate runtime error and remove all alarms from an object if it exceeds the per-object limit of alarms.&#10;- Add access checking on calls to `query_ip_ident()`.&#10;- Don't crash on redifinition of local variables where the second definition also has an assignment of a mismatched type.&#10;- Allow compilation without `SERVICE_PORT` defined.&#10;- Correct new super-snoop code.&#10;- Allow compilation without `ALLOWED_SERVICE` defined.&#10;- Change many `abort()` calls to `fatal()`.&#10;- Correct reference count handling for progs (code in objects).&#10;- Allow tracebacks through the VBFC object to complete.&#10;- Replaced indent code in &quot;ed&quot;; it would sometimes mangle the indented code.&#10;- Allow negative indices to `splice_array`; makes it possible to use code that says `array[2 .. -1]` to get everything from the 3rd element to the last element.&#10;&#10;## CD.04.03&#10;This release has two new features, one of which is commands with no trailing whitespace.  The can be used to implement eg. commands like : and ' with simple `add_action()` calls.  To use this feature, give `add_action()` a third argument of 2.  Note that with this change the handling of `add_action()` with a third argument of 1 has been changed to require a space between the verb and its arguments.  The latter is disabled by default for compatibility reasons.  This can be changed by editing config.h.&#10;&#10;The other new feature is a `query_trigverb()` efun which for abbreviated verbs like those added with a third argument of 1 to `add_action()` will return the whole verb, while `query_verb()` still returns only the portion that the player actually entered.&#10;&#10;Some bugs in handling of destructed objects.&#10;&#10;Getting the intersection of arrays (&quot;array &amp; array&quot;) and subtracting (&quot;array - array&quot;) now works, as does subtracting 0-elements from arrays of objects to remove destructed objects (&quot;obarray -= ({ 0 })&quot;).&#10;&#10;If various access check routines does not exist in the master object, they are now assumed to have been present and returned 1, allowing the operation.&#10;&#10;A problem where a '#pragma resident' statement in one object would cause all objects compiled after that object to appear to have set it has been fixed.&#10;'#pragma resident' should also work better now.  Parts of this from Alcides.&#10;&#10;The `break_string()` efun will now wrap the first line correctly; it used to have problems with that if a space between words was on the exact column the wrap was to be done on.  It will also work correctly with indentation now.&#10;&#10;The built-in wrapping of text sent to uses has been changed to also do word- wrapping on hyphens.&#10;&#10;The `rename()` efun has had its return values inverted so that it is consistent with return values for success and failure used by other efuns.&#10;&#10;If passed an array of object where an element were 0, `parse_command()` would try dereferencing an illegal pointer.&#10;&#10;The `sprintf()` efun could write into freed (and potentially reused) memory in certain instances.&#10;&#10;Alarms would be attached to the object holding the function to be called, and not the object creating the alarm.&#10;&#10;The driver will now give warnings if it encounters unknown #pragma statements.&#10;&#10;A destructor is now called with errors catched and ignored, allowing an object with a buggy destructor to actually be destructed.&#10;&#10;Line number information on runtime errors should be correct now.  From Alcides.&#10;&#10;Proper block-scoping of variables has been implemented.  Patches by Alcides.&#10;&#10;Protect against FP exceptions, flagging an error if one is caught.&#10;&#10;Protect against overflow when converting from float to integer in `ftoi()`.&#10;&#10;Enforce limit on line length in ed to prevent internal buffers from being overrun when a file is later listed.&#10;&#10;Optional second argument to `notify_fail()`; if set to 0 a message will not be set if one has already been set.&#10;&#10;Put in a limit on depth of inherit chain.&#10;&#10;Avoid copying a struct twice every time an LPC function returns; don't do any copying at all if there are no local variables in the LPC function.&#10;&#10;Make object-level profiling optional dependant on `PROFILE_OBJS`.&#10;&#10;Error handling redone to reduce copying of structures when error recovery contexts are needed; code is more intuitive and more effective.&#10;&#10;No longer require user to ensure that `F_OFFSET` is set up correctly for the yacc generator being used&#10;&#10;Better support for profiling the driver.&#10;&#10;cfun implementation of `sort_array()` added.&#10;&#10;`restart_mud` now processes any gprof output if the driver exited normally.&#10;&#10;We could sometimes call `recvfrom()` with invalid parameters.&#10;&#10;`efun::restore_mapping` failed on strings.&#10;&#10;`efun::parse_command` no longer fetch values from `/secure/master` on every call, only when `/secure/master` has been (re)loaded.&#10;&#10;`efun::call_out` removed.&#10;&#10;Many other minor performance improvements.&#10;&#10;Fixed some memory leaks.&#10;&#10;Fixed support for compiling with RANDOM&#10;&#10;Made it possible to link with the dmalloc debug allocator; do a much better job at freeing allocated memory if `DEALLOCATE_MEMORY_AT_SHUTDOWN` is defined.&#10;&#10;Inlined a few more functions for performance.&#10;&#10;Major cleanup of interpreter stack usage.&#10;&#10;Make a start on a regression test suite.  Run by doing &quot;make check&quot;.&#10;&#10;Fixed some swap leaks and cleaned up swap code. Changed swap code to use stdio; also keep an in-memory bitmap of free blocks in swap.  By Cygnus@Synergy.&#10;&#10;Add error checking on writes of precompiled binaries in `save_binary()`.&#10;&#10;Add FreeBSD as a supported platform for compilation.&#10;&#10;New string handling; by Cygnus@Synergy.&#10;&#10;Added '#pragma no_shadow' support.  If any object on the inherit chain has '#pragma no_shadow', shadowing the object will be denied.&#10;&#10;Push a return value on `efun::break_point()` so that the game won't crash if it is called.&#10;&#10;Add an option to decide wether or not using host names when sending UDP data- grams is allowed; disabled by default&#10;&#10;Redo handling of normal exit from a `catch()` statement so that `throw(0)` cannot be used to kill the driver&#10;&#10;## CD.04.00beta1&#10;This release contains several new things, of which some are not yet documented.&#10;The most important change is the addition of function references as data type.&#10;How they work is explained in various files in doc/function_references.&#10;&#10;An other addition is a new set of regexps. I don't have any docs yet, so you'll&#10;have to read the code to see how they work.&#10;&#10;An other important change is that the gamedriver has been further&#10;ANSI-fied. There should be very few warnings left when the driver is&#10;compiled with -Wall. Also, the varargs functions have been changed to be&#10;ANSI-C compliant.&#10;&#10;A manifest has been written, explaining the aims of development work in the&#10;near future. Please read it.&#10;&#10;## CD.03.31beta1&#10;The call_out system has changed again.&#10;`get_all_call_outs()` nad `delete_call_out()` are gone.&#10;`call_out()` still remains for compatibility purposes, but negative&#10;arguments are not legal.&#10;The new efuns `set_alarm()`, `get_alarm()`, `remove_alarm()` and `get_all_alarms()`&#10;implement the new call_out system.&#10;&#10;The driver calls the function `runtime_error()` in the master object when&#10;a runtime error is encountered. It is then up to the master object to&#10;send the information to the player/to the responsible wizard etc.&#10;&#10;The faulty `memmove-&gt;bcopy` macro from the previous release has been changed.&#10;&#10;## CD.03.27beta2&#10;The makefiles have been improved, with better macro handling.&#10;You need to install a new Makefile, or patch the old one.&#10;&#10;`make_func.c` no longer uses the 'make' program.&#10;&#10;A small error in line number information processing in `make_func.c` has been&#10;fixed.&#10;&#10;## CD.03.27beta1&#10;The Makefile has been split to make life easier.&#10;The file dependencies are in the file `Makefile.distrib`. The stuff that you&#10;may want to change, should be in a file called `Makefile`, that you have to&#10;create. The first time you create the Makefile, you should do this by moving&#10;the file `Makefile.default` to `Makefile` and then editing paths, compilation&#10;flags etc as required. After that, you should be able to take your old&#10;Makefile whenever you get a new version of the driver. If you need to make&#10;modifications, the README file will say so.&#10;&#10;Resets and heartbeat are no longer implemented by the driver. You should use&#10;the call_out mechanism instead. If you want to you can simulate resets and&#10;heartbeats in lpc.&#10;&#10;Explode works differently.&#10;If the start of the string matches the matching pattern, the first element&#10;in the resulting array will be an empty string. This was not previously the&#10;case.&#10;&#10;An example&#10;==========&#10;&#10;Old type explode:&#10;```&#10;arr = explode(&quot;aabcaade&quot;, &quot;aa&quot;)&#10;```&#10;&#10;    arr gets the value ({ &quot;bc&quot;, &quot;de&quot; })&#10;&#10;New type explode:&#10;&#10;```&#10;arr = explode(&quot;aabcaade&quot;, &quot;aa&quot;)&#10;```&#10;&#10;    arr gets the value ({ &quot;&quot;, &quot;bc&quot;, &quot;de&quot; })&#10;&#10;You can no longer have prototypes for functions that don't exist.&#10;&#10;The driver checks the number and types of function parameters in a&#10;better way.&#10;&#10;There are some new and nifty things that can be done with call_outs.&#10;Call_out overhead is smaller, and they are now stored in a per-object way.&#10;`remove_call_out()` is no longer an efun. You should simulate it in the mudlib.&#10;`find_call_out()` is no longer an efun. You should simulate it in the mudlib.&#10;`call_out()` now returns a number which is the id of the callout.&#10;`void delete_call_out(int id)` is used to remove a call_out.&#10;`mixed *get_all_call_outs()` returns an array of call_out info. One element&#10;per callout in the object.&#10;&#10;```&#10;mixed *calls = get_all_call_outs();&#10;int i;&#10;&#10;for (i = 0; i &lt; sizeof(calls); ++i) {&#10;int    id_of_callout = calls[i][0];&#10;string function      = calls[i][1]; // name of the callout function&#10;float  time_left     = calls[i][2]; // how soon until it gets called?&#10;float  delay         = calls[i][3]; // the original delay asked for&#10;// (might be negative)&#10;mixed  arg           = calls[i][4]; // Argument 3 to call_out().&#10;}&#10;```&#10;&#10;You can set the swapping parameters from within the game.&#10;You have to use `debug(&quot;set_swap&quot;)` to enable swapping.&#10;&#10;E.g.&#10;```&#10;debug(&quot;set_swap&quot;, ({ 0,         // Don't swap anything unless this much&#10;// memory is used. (bytes)&#10;16777216,  // Swap as much as possible if this much&#10;// memory is used. (bytes)&#10;60,        // Don't swap out an object/program&#10;// unless it is atleast this old. (seconds)&#10;900,       // Allways swap out an object/program if&#10;// it is older than this. (seconds)&#10;}));&#10;```&#10;also&#10;```&#10;debug(&quot;query_swap&quot;); /* or */ debug(&quot;query_swap&quot;, 0);&#10;// Returns the current swapping parameters.&#10;&#10;    debug(&quot;query_swap&quot;, 1); &#10;        // Returns swapin/swapout statistics as an (int ret[4][3])&#10;        // ({ // Objects in.&#10;        //    ({ /* last second */, /* last minute */, /* last hour */ }),&#10;              // Programs in.&#10;        //    ({ /* last second */, /* last minute */, /* last hour */ }),&#10;              // Objects out.&#10;        //    ({ /* last second */, /* last minute */, /* last hour */ }),&#10;              // Programs out.&#10;        //    ({ /* last second */, /* last minute */, /* last hour */ }),&#10;        // }) &#10;    debug(&quot;query_swap&quot;, 2);&#10;        // Returns age of oldest object/program not swapped out.&#10;&#10;    debug(&quot;query_swap&quot;, 3);&#10;       // Returns amount of used memory.&#10;```&#10;&#10;### Assertions&#10;&#10;Assertions (ordinary assertions, preconditions, postconditions, and invariants) are expressions that should evaluate to true. If not, they cause a runtime error when executed.&#10;&#10;- **Ordinary assertion:**&#10;  - `@ : expr;`  &#10;    Executed when control reaches it. Valid wherever a statement is.&#10;&#10;- **Precondition:**&#10;  - `&gt;&gt; : expr;`  &#10;    Executed after all arguments have values and before control reaches the function block.&#10;&#10;- **Postcondition:**&#10;  - `&lt;&lt; : expr;`  &#10;    Executed after a return statement but before control reaches the calling function. Valid between the function head and the function block.&#10;    &#10;    Example:&#10;    ```c&#10;    void reset()&#10;    // Pre/post-conditions here.&#10;    {&#10;      ...&#10;    }&#10;    ```&#10;&#10;- **Invariant:**&#10;  - `&lt;&gt; : expr;`  &#10;    Executed after a return statement but before control reaches the calling function. Valid where global declarations are.&#10;&#10;#### Assertion Bitmask&#10;&#10;Which assertions are executed is controlled with a 16-bit bitmask:&#10;- Assertions: bit 0 (`0x1`)&#10;- Preconditions: bit 8 (`0x100`)&#10;- Postconditions: bit 9 (`0x200`)&#10;- Invariants: bit 10 (`0x400`)&#10;&#10;#### Debug Assertion Control&#10;&#10;- `debug(&quot;set_debug_prog&quot;, prog, bitmask)`&#10;  - Sets which assertions to execute in the program `prog`.&#10;- `debug(&quot;query_debug_prog&quot;, prog)`&#10;  - Returns which assertions are being executed in program `prog`.&#10;- `debug(&quot;set_debug_obj&quot;, ob, bitmask)`&#10;  - Controls which assertions to execute in the object `ob`.&#10;- `debug(&quot;query_debug_obj&quot;, ob)`&#10;  - Returns which assertions are being executed in object `ob`.&#10;&#10;#### Alternate Assertion Form&#10;&#10;- `@ constant_bitmask : expr;`&#10;&#10;Constructors/destructors functions that are called when an object is&#10;created/destructed.&#10;A constructor is called before `create()` is called and after all constructors&#10;in inherited classes have been called.&#10;A destructor is called before the object is destructed and before any&#10;destructors in inherited classes have been called.&#10;The constructors and destructors are functions declared in a special way.&#10;they should have no return type and no arguments and must have a special name.&#10;&#10;constructor:&#10;```c&#10;&gt;&gt;()&#10;{&#10;/* construct object here */&#10;}&#10;```&#10;destructor:&#10;```c&#10;&lt;&lt;()&#10;{&#10;/* destruct the object here */&#10;}&#10;```&#10;&#10;The efuns `string val2str(mixed)` and `mixed str2val(string)`.&#10;`val2str()` converts the argument to its string represetation (the same&#10;as used with `save/restore_object()`).&#10;`str2val()` converts a value in string representation back to a value.&#10;&#10;The efuns `mapping m_save_object()` and `void m_restore_object(mapping)`&#10;`m_save_object()` saves its variables like `save_object` but returnes it&#10;in a mapping instead of writing it to a file.&#10;`m_restore_object()` restores an object but does it from its argument&#10;instead of from reading from a file.&#10;&#10;The efuns `void save_map(mapping, string)` and `mapping restrore_map(string)`.&#10;Write and read files like `save/restore_object` but take/return values&#10;in a mapping instead of the variables in `this_object()`.&#10;&#10;This means that `save/restore_object` can be simulated with&#10;```&#10;save_map(m_save_object(), &quot;file_name&quot;);&#10;```&#10;and&#10;```&#10;m_restore_object(restore_map(&quot;file_name&quot;));&#10;```&#10;&#10;Efuns `call_self()` and `call_selfv()`. Like `call_other()` and `call_otherv()`&#10;but makes a local call instead of a call_other.&#10;&#10;## CD.02.05&#10;This is the official release of the CD.02.05 driver. It is the final CD.02&#10;version. We will make extensive changes to the LPC language, to the reset,&#10;heart_beat and call_out systems in the next version, so we will be upping&#10;the version numbers to CD.03.XX. (Don't worry, the changes will require&#10;very minor muldib changes.)&#10;A version of the CD mudlib that should work with this driver version is&#10;released together with this driver version.&#10;Changes since the previous release:&#10;There are now working cfuns for a number of heavily used functions. They work&#10;with the CDLIB mudlib. The original lfuns are left intact in the mudlib&#10;release, so if you have problems with the cfuns, you can revert to the lfuns&#10;at any time.&#10;The rusage calls have been fixed, so now you can use `top_ten_cpu` and&#10;`getprofile` options to the debug() efun. (If you have enabled the options in&#10;the config file)&#10;There is now documentation for all the efuns (I hope). The documentation is&#10;distributed with the mudlib. In the future I plan to distribute the efundocs&#10;separately.&#10;The error logs look neater.&#10;Hname now supports the user identification protocol.&#10;A bunch of minor bugs have been fixed. Among them some obscure bugs in&#10;`parse_command`. There are a few known bugs remaining. The most important&#10;ones are that very long lines sent to `write_socket` hang the driver and that&#10;virtual inheritance doesn't work properly under some circumstances.&#10;&#10;### Current compatibility status&#10;| Architecture | OS                | Compiler | Status        |&#10;|--------------|-------------------|----------|---------------|&#10;| Sparc        | SunOS 4           | gcc      | OK            |&#10;| Sparc        | SunOS 5 (SOLARIS) | gcc      | OK            |&#10;| Alpha        | OSF/1              | cc       | OK            |&#10;| ??           | Sequent           | ??       | Probably OK   |&#10;| 386          | Linux             | gcc      | OK            |&#10;| 386          | NetBSD            | gcc      | Crashes       |&#10;| MIPS         | Ultrix 4.2        | gcc      | OK            |&#10;In addition to this the driver will probably work well on a lot of&#10;Unixes, but we haven't made any recent tests.&#10;## the first beta release of the CD.02.03 driver&#10;Changes since the previous release:&#10;Some bug fixes.&#10;Speedup of mappings.&#10;`interpret.c` has been rewritten to get rid of the huge switch(). It started&#10;to cause compiler problems.&#10;Compat changes for Solaris 2.x and Sequent.&#10;A bunch of cfuns for use with the CDLIB have been added. They are untested.&#10;New malloc called `bibopmalloc`. This malloc uses less memory than smalloc, but&#10;is slightly slower. It needs some more tuning before it's prefect on&#10;32 bit machines, it works very well on 64 bit machines.&#10;`Process_string` and `process_value` will now accept an extra argument. If this&#10;argument is non-zero, the call will set `previous_object` to the object&#10;returned by a call to the function `get_vbfc_object` in the master object.&#10;This is a way to provide vbfc security without the overhead of changing&#10;uids and euids. It also avoids the problems with errors during vbfc calls.&#10;&#10;## the third beta release of the CD.02.02 driver.&#10;&#10;The differences from the previous beta versions are that I fixed the little&#10;makefile problem, and that cfuns now should work properly. The ones that I have&#10;tested with cdlib are `write`, `check_call` and `query_prop`. The others were&#10;used with the kingdoms mudlib, and are probably not working they way&#10;you want them to.&#10;&#10;There is a new release of byacc out. (Berkely Yacc) It is faster than both&#10;yacc and bison. (Compiles faster and creates faster code.) You can get&#10;a bit better speed by installing it and changing the makefile to use it instead&#10;of bison.&#10;&#10;If you want to try your hands at coding cfuns the following functions are&#10;strong candidates:&#10;&#10;In `/secure/simul_efun`:&#10;`cat`&#10;`sort_array`&#10;`tell_room`&#10;`say` (expand the tell_rooms inside the say)&#10;&#10;In `/std/object`:&#10;`add_prop`&#10;`change_prop`&#10;`add_list`&#10;`del_list`&#10;`query_list`&#10;`item_id`&#10;`cmditem_action`&#10;" />
              <option name="updatedContent" value="# Changelog&#10;&#10;## CD.04.06&#10;- Fixed small things that were pointed out by `GCC 2.8.x`&#10;- Fixed one more memory leak, pointed out by `Cygnus`&#10;- Changed how sockets are made non-blocking to not repeat common code many times.&#10;- Make `array - ({ 0 })` and `-= ({ 0 })` expressions actually remove all destructed objects in arrays too; from `Cygnus`&#10;- Added statistics for cleanup performance; from `Cygnus`&#10;- Made `?:` expressions with an empty second part legal, if empty it will take on the value from the first part; from `Azid@Kingdoms`.&#10;&#10;## CD.04.05&#10;- Made `functionp()` return 0 when passed a pointer to a function in a destructed object.&#10;- Added `function_object()` to extract the object from a function pointer.&#10;- After a catch of evaluation cost overflow the evaluation cost is reset to slightly below maximum if this was in the outermost `catch()`. The reserve is defined by `EXTRA_COST` in `config.h`.&#10;- Allow using parenthesized constant strings in inherit; from `Plarry@Muddy_Waters`.&#10;- Put back code to call `clean_up()` in objects before swapping them out, to give them a chance to remove themselves.&#10;- Added an auto-inherited object `/secure/auto`. This object is automatically inherited into _all_ objects, including `/secure/master`. Most of the code by `Azid@Kingdoms`&#10;- Added `efun::min()` and `efun::max()`; from `Azid`&#10;- Added `efun::function_name()` to get a string representation of a function pointer.&#10;- Made the not-operator `!` understand about destructed objects and function pointers pointing at functions in destructed objects.&#10;- Removed `efun::cindent()` and `util/indent`; it was both sorely out of date and did not make much sense to keep.&#10;- Removed `efun::query_inherit_list()`; it is available as `efun::debug(&quot;inherit_list&quot;)`&#10;- Removed the reserved word and attribute `protected`; it was not used.&#10;- Told the indenter in `ed` about some preprocessor directives; from `Skywise`&#10;- Moved function composition into the driver; from `Cygnus@Synergy`.&#10;- `operator(!)` and `operator(~)` now works; from `Skywise`&#10;- Get better textual function pointer representations; from `Skywise`&#10;- Allow master to insert lines at the start of new files; from `Skywise`&#10;- Optimized jumps in loops and switch statements; from `Skywise`&#10;- Optimized jumps to jumps; from `Cygnus`&#10;- Use `snprintf()` many places to prevent overflowing buffers; add a small version of `snprintf()` for systems that might not have it.  Add regression test for the problem this fixes.&#10;- Only enable `efun::break_point()` when the driver is compiled with `DEBUG`&#10;- Added floating point formats to `efun::speintf()`; from `Pippin@Synergy`&#10;- Corrected a seldom-occuring condition where we would loop forever.&#10;- Don't print last line twice in `ed` when doing substitution if both the general print flag is set and the &quot;p&quot; flag is given on the substitute command.&#10;- Define `DEBUG_DRIVER` preprocessor symbol if driver has been compiled with `DEBUG` enabled.&#10;- Make `efun::min()` and `efun::max()` take a variable number of arguments, at least two, and return the minimum/maximum of all values passed.  From `Cygnus@Synergy`.&#10;- Make the `|` operator do array unions; from `Cygnus@Synergy`.&#10;- Check for stack overflow in macro version of `push_svalue()`.&#10;- Don't die if a function is redeclared with different number of arguments when running in `DEBUG` mode.&#10;- Don't special-case shadowing of `catch_tell()` in the driver; that should be done in the mudlib if at all.&#10;- `efun::call_self` and `efun::call_selfv` no longer cause the stack to be mangled, restore them and add a regression test to verify that they work properly&#10;- Add implementation of `strerror()` for systems that do not provide it&#10;&#10;## CD.04.04&#10;- Fixed a problem where the driver would crash when getting the command list for a living object if any of the verbs had an efun connected to them (and thus no object defined)&#10;- Optimized `SUPER_SNOOP` code; from `Azid`.&#10;- Allow `void` as an argument, indicating no arguments are allowed like ANSI. From `Azid`.&#10;- Made `sprintf()` generate runtime errors when buffer overflows.&#10;- Add limits to number of concurrent alarms per object; default to 128.&#10;- Generate runtime error and remove all alarms from an object if it exceeds the per-object limit of alarms.&#10;- Add access checking on calls to `query_ip_ident()`.&#10;- Don't crash on redifinition of local variables where the second definition also has an assignment of a mismatched type.&#10;- Allow compilation without `SERVICE_PORT` defined.&#10;- Correct new super-snoop code.&#10;- Allow compilation without `ALLOWED_SERVICE` defined.&#10;- Change many `abort()` calls to `fatal()`.&#10;- Correct reference count handling for progs (code in objects).&#10;- Allow tracebacks through the `VBFC` object to complete.&#10;- Replaced indent code in `ed`; it would sometimes mangle the indented code.&#10;- Allow negative indices to `splice_array`; makes it possible to use code that says `array[2 .. -1]` to get everything from the 3rd element to the last element.&#10;&#10;## CD.04.03&#10;This release has two new features, one of which is commands with no trailing whitespace.  The can be used to implement eg. commands like : and ' with simple `add_action()` calls.  To use this feature, give `add_action()` a third argument of 2.  Note that with this change the handling of `add_action()` with a third argument of 1 has been changed to require a space between the verb and its arguments.  The latter is disabled by default for compatibility reasons.  This can be changed by editing `config.h`.&#10;&#10;The other new feature is a `query_trigverb()` efun which for abbreviated verbs like those added with a third argument of 1 to `add_action()` will return the whole verb, while `query_verb()` still returns only the portion that the player actually entered.&#10;&#10;Some bugs in handling of destructed objects.&#10;&#10;Getting the intersection of arrays (&quot;array &amp; array&quot;) and subtracting (&quot;array - array&quot;) now works, as does subtracting 0-elements from arrays of objects to remove destructed objects (&quot;obarray -= ({ 0 })&quot;).&#10;&#10;If various access check routines does not exist in the master object, they are now assumed to have been present and returned 1, allowing the operation.&#10;&#10;A problem where a '#pragma resident' statement in one object would cause all objects compiled after that object to appear to have set it has been fixed.&#10;'#pragma resident' should also work better now.  Parts of this from Alcides.&#10;&#10;The `break_string()` efun will now wrap the first line correctly; it used to have problems with that if a space between words was on the exact column the wrap was to be done on.  It will also work correctly with indentation now.&#10;&#10;The built-in wrapping of text sent to uses has been changed to also do word- wrapping on hyphens.&#10;&#10;The `rename()` efun has had its return values inverted so that it is consistent with return values for success and failure used by other efuns.&#10;&#10;If passed an array of object where an element were 0, `parse_command()` would try dereferencing an illegal pointer.&#10;&#10;The `sprintf()` efun could write into freed (and potentially reused) memory in certain instances.&#10;&#10;Alarms would be attached to the object holding the function to be called, and not the object creating the alarm.&#10;&#10;The driver will now give warnings if it encounters unknown #pragma statements.&#10;&#10;A destructor is now called with errors catched and ignored, allowing an object with a buggy destructor to actually be destructed.&#10;&#10;Line number information on runtime errors should be correct now.  From Alcides.&#10;&#10;Proper block-scoping of variables has been implemented.  Patches by Alcides.&#10;&#10;Protect against FP exceptions, flagging an error if one is caught.&#10;&#10;Protect against overflow when converting from float to integer in `ftoi()`.&#10;&#10;Enforce limit on line length in ed to prevent internal buffers from being overrun when a file is later listed.&#10;&#10;Optional second argument to `notify_fail()`; if set to 0 a message will not be set if one has already been set.&#10;&#10;Put in a limit on depth of inherit chain.&#10;&#10;Avoid copying a struct twice every time an LPC function returns; don't do any copying at all if there are no local variables in the LPC function.&#10;&#10;Make object-level profiling optional dependant on `PROFILE_OBJS`.&#10;&#10;Error handling redone to reduce copying of structures when error recovery contexts are needed; code is more intuitive and more effective.&#10;&#10;No longer require user to ensure that `F_OFFSET` is set up correctly for the yacc generator being used&#10;&#10;Better support for profiling the driver.&#10;&#10;cfun implementation of `sort_array()` added.&#10;&#10;`restart_mud` now processes any gprof output if the driver exited normally.&#10;&#10;We could sometimes call `recvfrom()` with invalid parameters.&#10;&#10;`efun::restore_mapping` failed on strings.&#10;&#10;`efun::parse_command` no longer fetch values from `/secure/master` on every call, only when `/secure/master` has been (re)loaded.&#10;&#10;`efun::call_out` removed.&#10;&#10;Many other minor performance improvements.&#10;&#10;Fixed some memory leaks.&#10;&#10;Fixed support for compiling with RANDOM&#10;&#10;Made it possible to link with the dmalloc debug allocator; do a much better job at freeing allocated memory if `DEALLOCATE_MEMORY_AT_SHUTDOWN` is defined.&#10;&#10;Inlined a few more functions for performance.&#10;&#10;Major cleanup of interpreter stack usage.&#10;&#10;Make a start on a regression test suite.  Run by doing &quot;make check&quot;.&#10;&#10;Fixed some swap leaks and cleaned up swap code. Changed swap code to use stdio; also keep an in-memory bitmap of free blocks in swap.  By Cygnus@Synergy.&#10;&#10;Add error checking on writes of precompiled binaries in `save_binary()`.&#10;&#10;Add FreeBSD as a supported platform for compilation.&#10;&#10;New string handling; by Cygnus@Synergy.&#10;&#10;Added '#pragma no_shadow' support.  If any object on the inherit chain has '#pragma no_shadow', shadowing the object will be denied.&#10;&#10;Push a return value on `efun::break_point()` so that the game won't crash if it is called.&#10;&#10;Add an option to decide wether or not using host names when sending UDP data- grams is allowed; disabled by default&#10;&#10;Redo handling of normal exit from a `catch()` statement so that `throw(0)` cannot be used to kill the driver&#10;&#10;## CD.04.00beta1&#10;This release contains several new things, of which some are not yet documented.&#10;The most important change is the addition of function references as data type.&#10;How they work is explained in various files in doc/function_references.&#10;&#10;An other addition is a new set of regexps. I don't have any docs yet, so you'll&#10;have to read the code to see how they work.&#10;&#10;An other important change is that the gamedriver has been further&#10;ANSI-fied. There should be very few warnings left when the driver is&#10;compiled with -Wall. Also, the varargs functions have been changed to be&#10;ANSI-C compliant.&#10;&#10;A manifest has been written, explaining the aims of development work in the&#10;near future. Please read it.&#10;&#10;## CD.03.31beta1&#10;The call_out system has changed again.&#10;`get_all_call_outs()` nad `delete_call_out()` are gone.&#10;`call_out()` still remains for compatibility purposes, but negative&#10;arguments are not legal.&#10;The new efuns `set_alarm()`, `get_alarm()`, `remove_alarm()` and `get_all_alarms()`&#10;implement the new call_out system.&#10;&#10;The driver calls the function `runtime_error()` in the master object when&#10;a runtime error is encountered. It is then up to the master object to&#10;send the information to the player/to the responsible wizard etc.&#10;&#10;The faulty `memmove-&gt;bcopy` macro from the previous release has been changed.&#10;&#10;## CD.03.27beta2&#10;The makefiles have been improved, with better macro handling.&#10;You need to install a new Makefile, or patch the old one.&#10;&#10;`make_func.c` no longer uses the 'make' program.&#10;&#10;A small error in line number information processing in `make_func.c` has been&#10;fixed.&#10;&#10;## CD.03.27beta1&#10;The Makefile has been split to make life easier.&#10;The file dependencies are in the file `Makefile.distrib`. The stuff that you&#10;may want to change, should be in a file called `Makefile`, that you have to&#10;create. The first time you create the Makefile, you should do this by moving&#10;the file `Makefile.default` to `Makefile` and then editing paths, compilation&#10;flags etc as required. After that, you should be able to take your old&#10;Makefile whenever you get a new version of the driver. If you need to make&#10;modifications, the README file will say so.&#10;&#10;Resets and heartbeat are no longer implemented by the driver. You should use&#10;the call_out mechanism instead. If you want to you can simulate resets and&#10;heartbeats in lpc.&#10;&#10;Explode works differently.&#10;If the start of the string matches the matching pattern, the first element&#10;in the resulting array will be an empty string. This was not previously the&#10;case.&#10;&#10;An example&#10;==========&#10;&#10;Old type explode:&#10;```&#10;arr = explode(&quot;aabcaade&quot;, &quot;aa&quot;)&#10;```&#10;&#10;    arr gets the value ({ &quot;bc&quot;, &quot;de&quot; })&#10;&#10;New type explode:&#10;&#10;```&#10;arr = explode(&quot;aabcaade&quot;, &quot;aa&quot;)&#10;```&#10;&#10;    arr gets the value ({ &quot;&quot;, &quot;bc&quot;, &quot;de&quot; })&#10;&#10;You can no longer have prototypes for functions that don't exist.&#10;&#10;The driver checks the number and types of function parameters in a&#10;better way.&#10;&#10;There are some new and nifty things that can be done with call_outs.&#10;Call_out overhead is smaller, and they are now stored in a per-object way.&#10;`remove_call_out()` is no longer an efun. You should simulate it in the mudlib.&#10;`find_call_out()` is no longer an efun. You should simulate it in the mudlib.&#10;`call_out()` now returns a number which is the id of the callout.&#10;`void delete_call_out(int id)` is used to remove a call_out.&#10;`mixed *get_all_call_outs()` returns an array of call_out info. One element&#10;per callout in the object.&#10;&#10;```&#10;mixed *calls = get_all_call_outs();&#10;int i;&#10;&#10;for (i = 0; i &lt; sizeof(calls); ++i) {&#10;int    id_of_callout = calls[i][0];&#10;string function      = calls[i][1]; // name of the callout function&#10;float  time_left     = calls[i][2]; // how soon until it gets called?&#10;float  delay         = calls[i][3]; // the original delay asked for&#10;// (might be negative)&#10;mixed  arg           = calls[i][4]; // Argument 3 to call_out().&#10;}&#10;```&#10;&#10;You can set the swapping parameters from within the game.&#10;You have to use `debug(&quot;set_swap&quot;)` to enable swapping.&#10;&#10;E.g.&#10;```&#10;debug(&quot;set_swap&quot;, ({ 0,         // Don't swap anything unless this much&#10;// memory is used. (bytes)&#10;16777216,  // Swap as much as possible if this much&#10;// memory is used. (bytes)&#10;60,        // Don't swap out an object/program&#10;// unless it is atleast this old. (seconds)&#10;900,       // Allways swap out an object/program if&#10;// it is older than this. (seconds)&#10;}));&#10;```&#10;also&#10;```&#10;debug(&quot;query_swap&quot;); /* or */ debug(&quot;query_swap&quot;, 0);&#10;// Returns the current swapping parameters.&#10;&#10;    debug(&quot;query_swap&quot;, 1); &#10;        // Returns swapin/swapout statistics as an (int ret[4][3])&#10;        // ({ // Objects in.&#10;        //    ({ /* last second */, /* last minute */, /* last hour */ }),&#10;              // Programs in.&#10;        //    ({ /* last second */, /* last minute */, /* last hour */ }),&#10;              // Objects out.&#10;        //    ({ /* last second */, /* last minute */, /* last hour */ }),&#10;              // Programs out.&#10;        //    ({ /* last second */, /* last minute */, /* last hour */ }),&#10;        // }) &#10;    debug(&quot;query_swap&quot;, 2);&#10;        // Returns age of oldest object/program not swapped out.&#10;&#10;    debug(&quot;query_swap&quot;, 3);&#10;       // Returns amount of used memory.&#10;```&#10;&#10;### Assertions&#10;&#10;Assertions (ordinary assertions, preconditions, postconditions, and invariants) are expressions that should evaluate to true. If not, they cause a runtime error when executed.&#10;&#10;- **Ordinary assertion:**&#10;  - `@ : expr;`  &#10;    Executed when control reaches it. Valid wherever a statement is.&#10;&#10;- **Precondition:**&#10;  - `&gt;&gt; : expr;`  &#10;    Executed after all arguments have values and before control reaches the function block.&#10;&#10;- **Postcondition:**&#10;  - `&lt;&lt; : expr;`  &#10;    Executed after a return statement but before control reaches the calling function. Valid between the function head and the function block.&#10;    &#10;    Example:&#10;    ```c&#10;    void reset()&#10;    // Pre/post-conditions here.&#10;    {&#10;      ...&#10;    }&#10;    ```&#10;&#10;- **Invariant:**&#10;  - `&lt;&gt; : expr;`  &#10;    Executed after a return statement but before control reaches the calling function. Valid where global declarations are.&#10;&#10;#### Assertion Bitmask&#10;&#10;Which assertions are executed is controlled with a 16-bit bitmask:&#10;- Assertions: bit 0 (`0x1`)&#10;- Preconditions: bit 8 (`0x100`)&#10;- Postconditions: bit 9 (`0x200`)&#10;- Invariants: bit 10 (`0x400`)&#10;&#10;#### Debug Assertion Control&#10;&#10;- `debug(&quot;set_debug_prog&quot;, prog, bitmask)`&#10;  - Sets which assertions to execute in the program `prog`.&#10;- `debug(&quot;query_debug_prog&quot;, prog)`&#10;  - Returns which assertions are being executed in program `prog`.&#10;- `debug(&quot;set_debug_obj&quot;, ob, bitmask)`&#10;  - Controls which assertions to execute in the object `ob`.&#10;- `debug(&quot;query_debug_obj&quot;, ob)`&#10;  - Returns which assertions are being executed in object `ob`.&#10;&#10;#### Alternate Assertion Form&#10;&#10;- `@ constant_bitmask : expr;`&#10;&#10;Constructors/destructors functions that are called when an object is&#10;created/destructed.&#10;A constructor is called before `create()` is called and after all constructors&#10;in inherited classes have been called.&#10;A destructor is called before the object is destructed and before any&#10;destructors in inherited classes have been called.&#10;The constructors and destructors are functions declared in a special way.&#10;they should have no return type and no arguments and must have a special name.&#10;&#10;constructor:&#10;```c&#10;&gt;&gt;()&#10;{&#10;/* construct object here */&#10;}&#10;```&#10;destructor:&#10;```c&#10;&lt;&lt;()&#10;{&#10;/* destruct the object here */&#10;}&#10;```&#10;&#10;The efuns `string val2str(mixed)` and `mixed str2val(string)`.&#10;`val2str()` converts the argument to its string represetation (the same&#10;as used with `save/restore_object()`).&#10;`str2val()` converts a value in string representation back to a value.&#10;&#10;The efuns `mapping m_save_object()` and `void m_restore_object(mapping)`&#10;`m_save_object()` saves its variables like `save_object` but returnes it&#10;in a mapping instead of writing it to a file.&#10;`m_restore_object()` restores an object but does it from its argument&#10;instead of from reading from a file.&#10;&#10;The efuns `void save_map(mapping, string)` and `mapping restrore_map(string)`.&#10;Write and read files like `save/restore_object` but take/return values&#10;in a mapping instead of the variables in `this_object()`.&#10;&#10;This means that `save/restore_object` can be simulated with&#10;```&#10;save_map(m_save_object(), &quot;file_name&quot;);&#10;```&#10;and&#10;```&#10;m_restore_object(restore_map(&quot;file_name&quot;));&#10;```&#10;&#10;Efuns `call_self()` and `call_selfv()`. Like `call_other()` and `call_otherv()`&#10;but makes a local call instead of a call_other.&#10;&#10;## CD.02.05&#10;This is the official release of the CD.02.05 driver. It is the final CD.02&#10;version. We will make extensive changes to the LPC language, to the reset,&#10;heart_beat and call_out systems in the next version, so we will be upping&#10;the version numbers to CD.03.XX. (Don't worry, the changes will require&#10;very minor muldib changes.)&#10;A version of the CD mudlib that should work with this driver version is&#10;released together with this driver version.&#10;Changes since the previous release:&#10;There are now working cfuns for a number of heavily used functions. They work&#10;with the CDLIB mudlib. The original lfuns are left intact in the mudlib&#10;release, so if you have problems with the cfuns, you can revert to the lfuns&#10;at any time.&#10;The rusage calls have been fixed, so now you can use `top_ten_cpu` and&#10;`getprofile` options to the debug() efun. (If you have enabled the options in&#10;the config file)&#10;There is now documentation for all the efuns (I hope). The documentation is&#10;distributed with the mudlib. In the future I plan to distribute the efundocs&#10;separately.&#10;The error logs look neater.&#10;Hname now supports the user identification protocol.&#10;A bunch of minor bugs have been fixed. Among them some obscure bugs in&#10;`parse_command`. There are a few known bugs remaining. The most important&#10;ones are that very long lines sent to `write_socket` hang the driver and that&#10;virtual inheritance doesn't work properly under some circumstances.&#10;&#10;### Current compatibility status&#10;| Architecture | OS                | Compiler | Status        |&#10;|--------------|-------------------|----------|---------------|&#10;| Sparc        | SunOS 4           | gcc      | OK            |&#10;| Sparc        | SunOS 5 (SOLARIS) | gcc      | OK            |&#10;| Alpha        | OSF/1              | cc       | OK            |&#10;| ??           | Sequent           | ??       | Probably OK   |&#10;| 386          | Linux             | gcc      | OK            |&#10;| 386          | NetBSD            | gcc      | Crashes       |&#10;| MIPS         | Ultrix 4.2        | gcc      | OK            |&#10;In addition to this the driver will probably work well on a lot of&#10;Unixes, but we haven't made any recent tests.&#10;## the first beta release of the CD.02.03 driver&#10;Changes since the previous release:&#10;Some bug fixes.&#10;Speedup of mappings.&#10;`interpret.c` has been rewritten to get rid of the huge switch(). It started&#10;to cause compiler problems.&#10;Compat changes for Solaris 2.x and Sequent.&#10;A bunch of cfuns for use with the CDLIB have been added. They are untested.&#10;New malloc called `bibopmalloc`. This malloc uses less memory than smalloc, but&#10;is slightly slower. It needs some more tuning before it's prefect on&#10;32 bit machines, it works very well on 64 bit machines.&#10;`Process_string` and `process_value` will now accept an extra argument. If this&#10;argument is non-zero, the call will set `previous_object` to the object&#10;returned by a call to the function `get_vbfc_object` in the master object.&#10;This is a way to provide vbfc security without the overhead of changing&#10;uids and euids. It also avoids the problems with errors during vbfc calls.&#10;&#10;## the third beta release of the CD.02.02 driver.&#10;&#10;The differences from the previous beta versions are that I fixed the little&#10;makefile problem, and that cfuns now should work properly. The ones that I have&#10;tested with cdlib are `write`, `check_call` and `query_prop`. The others were&#10;used with the kingdoms mudlib, and are probably not working they way&#10;you want them to.&#10;&#10;There is a new release of byacc out. (Berkely Yacc) It is faster than both&#10;yacc and bison. (Compiles faster and creates faster code.) You can get&#10;a bit better speed by installing it and changing the makefile to use it instead&#10;of bison.&#10;&#10;If you want to try your hands at coding cfuns the following functions are&#10;strong candidates:&#10;&#10;In `/secure/simul_efun`:&#10;`cat`&#10;`sort_array`&#10;`tell_room`&#10;`say` (expand the tell_rooms inside the say)&#10;&#10;In `/std/object`:&#10;`add_prop`&#10;`change_prop`&#10;`add_list`&#10;`del_list`&#10;`query_list`&#10;`item_id`&#10;`cmditem_action`&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/INSTALL.md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/INSTALL.md" />
              <option name="updatedContent" value="# Installation Guide&#10;&#10;This is a brief description of how to install the CD driver:&#10;&#10;1. **Create Directories**&#10;   - Make a directory `mud`, which contains directories named `src`, `bin`, and `lib`.&#10;   - Move the game driver source (this code) to the `src` directory.&#10;&#10;2. **Prepare Makefile**&#10;   - Copy `Makefile.default` to `Makefile` and uncomment the proper definitions for compiling on your system.&#10;   - If your system is not one of those listed, you will have to create a section for it.&#10;   - Try compiling the driver with empty `MISSING_OBJ` and `MISSING_SRC` variables; add only objects required to successfully compile the driver.&#10;   - Pay special attention to the version of yacc being used. **AT&amp;T yacc will NOT work.** `byacc` (standard on some systems) is the preferred yacc to use, `GNU bison` will also work.&#10;&#10;3. **Set Paths**&#10;   - Make sure the `MUD_LIB` and `BINDIR` variables in your Makefile reflect where the mud's binaries and mudlib will be placed.&#10;&#10;4. **Allocator Options**&#10;   - There are several allocators supported by the driver. The preferred one is `bibop`, and is also the default.&#10;&#10;5. **Development Options**&#10;   - If you are going to do driver development, it is recommended that you enable the `DEBUG` option in your Makefile as well.&#10;   - You may want to look over the options in `config.h` and tailor those for your system.&#10;&#10;6. **Build the Driver**&#10;   - To actually build the driver, run:&#10;     ```sh&#10;     make&#10;     make utils&#10;     ```&#10;   - To run a regression test of the driver before installing, run:&#10;     ```sh&#10;     make check&#10;     ```&#10;&#10;7. **Install the Driver**&#10;   - To install the driver, run:&#10;     ```sh&#10;     make install&#10;     make install.utils&#10;     ```&#10;&#10;8. **Install Mudlib**&#10;   - Get a mudlib and install it in the `lib` directory. The most current version of the CDLIB mudlib is available from `ftp.cd.chalmers.se` in the directory `pub/cdlib`.&#10;&#10;9. **Test the Game**&#10;   - Test the game with:&#10;     ```sh&#10;     driver &amp;&#10;     ```&#10;   - If you see the message `Setting up ipc`, then you are up and running.&#10;   - Test with:&#10;     ```sh&#10;     telnet localhost 3011&#10;     ```&#10;     (or the port number you specified in the `config.h` file, if you changed it)&#10;&#10;10. **Automatic Restart**&#10;    - If you want the game to restart automatically, use:&#10;      ```sh&#10;      bin/restart_mud&#10;      ```&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/LICENSE.md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/LICENSE.md" />
              <option name="updatedContent" value="# License&#10;&#10;The CD gamedriver, consisting of code in this directory and subdirectories, as made available from the authors, is copyright Chalmers Datorförening, Göteborg, Sweden 1991, 1992, 1993. Parts of the package may be copyrighted by others or may be in the public domain. In these cases such information is stated in the individual files.&#10;&#10;The CD gamedriver is based on LPMud, developed by Lars Pensjö and others.&#10;&#10;Source code herein refers to the source code, and any executables created from the same source code.&#10;&#10;All rights reserved. Permission is granted to extend and modify the source code provided subject to the restriction that the source code may not be used in any way whatsoever for monetary gain.&#10;&#10;The grants made here may be subject to later change. Such a change will only apply to versions of the program made available to the public after said change.&#10;&#10;Note that the modules `mstring.c` and `mstring.h` carry a separate copyright and limits on use. Read those files for details.&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/README.md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/README.md" />
              <option name="updatedContent" value="# LPMud Arkadia Distribution&#10;&#10;This distribution includes a mudlib and driver, both extended from the original CD mudlib and driver. The latest versions of the originals can be found at [Genesis MUD](http://genesis.cs.chalmers.se).&#10;&#10;## Polish Language Support&#10;&#10;Some modifications focus on supporting Polish grammar, mainly in the `parse.c` file of the driver. However, Polish support is only a small part of the overall changes. Enhancements have been made to improve game mechanics, functionality for developers, and to fix bugs from the original authors.&#10;&#10;## Development and Sharing&#10;&#10;The mudlib and driver have been developed on Arkadia. We share our work to accelerate the development of this codebase. We hope to be informed about interesting changes made to the driver or mudlib on other MUDs that benefit from our work.&#10;&#10;The latest version of this distribution is available at:&#10;&#10;`ftp://arkadia@ftp.arkadia.rpg.pl`&#10;&#10;## Installation Instructions&#10;&#10;Driver sources are located in the `src` subdirectory. To install the MUD:&#10;&#10;1. Edit the following files:&#10;   - `src/config.h` (mainly the port number)&#10;   - `src/Makefile` (paths, system type, parser, compilation options)&#10;2. Run the following commands in order:&#10;   - `make`&#10;   - `make install`&#10;   - `make install_utils`&#10;3. To start the MUD, run `bin/restart_mud` and connect via telnet to the port set in `config.h` (default: 2300).&#10;4. Log in as the wizard character named `root` (no password required); this account has maximum privileges.&#10;&#10;## Troubleshooting&#10;&#10;If you encounter problems, please do not bother wizards from any MUD. Try to find a solution yourself. Creating a new MUD involves many more difficult stages; if you cannot overcome this one, it may not be worth engaging in such a large project.&#10;&#10;If you are determined enough, we wish you the best of luck!&#10;&#10;**Alvin@Arkadia**&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>